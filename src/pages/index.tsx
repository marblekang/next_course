import React from "react";
import Link, { LinkProps } from "next/link";
/**
 * @description
 * - next에서는 export default 해줘야함
 */
const Homepage = () => {
  return <div>Home</div>;
};

export default Homepage;
